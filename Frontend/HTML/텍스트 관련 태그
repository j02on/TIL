1. 제목 태그: <h1> ~ <h6>까지 있다.
*검색 엔진은 제목 태그를 중요한 의미로 받아들일 가능성이 크다.

2. 글자 형태 태그
<b>: 글자 진하게
<strong>: 표현되는 외양이 <b>와 똑같다. 웹표준을 준수하고자 한다면 strong를 사용하는 것이 바람직ㅎ다.
<i>: Italic체를 지정
<em>: 강조, 중요한 text를 지정한다. 의미론적 중요성의 의미를 갖는다.
<small>: small text를 지정(크기가 작다)
<mark>: highlighted text를 지정(형광펜 같은 느낌)
<del>: deleted text를 지정(글자 위에 줄 그어논 것처럼)
<ins>: inserted text를 지정(글자 밑에 줄 그어논 것처럼)
<sub>: subscripted text를 지정(아래에 쓰인)
<sup>: superscripted text를 지정(위에 쓰인)

3. 본문 태그
<p>: 단락(paragraphs)를 지정 (본문 내용)
<br>: br tag는 개행을 지정한다(enter), br tag는 빈요소로 종료태그가 없다.
<pre>: 형식화된 text를 지정, pre 태그 내의 content는 작성된 그대로 브라우저에 표시된다.(<br>tag 없이도 enter가 표시됨)
<hr>: 수평줄을 삽입
<q>: 짧은 인용문을 지정, 브라우저는 인용부호(큰따옴표/quotation marks)로 q 요소를 감싼다.
-> <q>를 하면 ""이 앞뒤에 표시
<blockquote>: 긴 인용문 블록을 지정(들여쓰기를 해줌)
*css를 이용해 다양한 style을 적용할 수 있다.
