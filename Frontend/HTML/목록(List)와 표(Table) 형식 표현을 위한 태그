1. 목록: 순서 없는 목록과 순서 있는 목록, 중첩 목록이 있다.
-순서 없는 목록: <ul></ul>을 사용한다.(<li>를 사용해 각각 목록을 표시해 줌)
-순서 있는 목록: <ol></ol>을 사용한다.(<ol>도 위와 같이 <li>를 사용한다.)
*<ol>의 type 어트리뷰트
    -"1": 숫자 (기본값)
    -"A": 대문자 알파벳
    -"a": 소문자 알파벳
    -"I": 대문자 로마숫자
    -"i": 소문자 로마숫자
**start 어트리뷰트로 리스트의 시작값을 지정 가능하다
**reversed 어트리뷰트를 지정하면 리스트의 순서값을 역으로 표현

-중첩 목록: 목록이 중첩되어 있는 것(<ol>, <ul>같은 것들은 for문처럼 중첩하면 됨)

2. 테이블: 표를 만들 때 사용하는 태그, 주로 공간 분할 태그인 div를 사용하여 레이아웃을 구성한다.
-<table>: 표를 감싸는 태그
*table 태그의 어트리뷰트
    -border: 표 테두리 두께 지정
    -rowspan: 해당 셀이 점유하는 행의 수 지정(행끼리 합친다.)
    -colspan: 해당 셀이 점유하는 열의 수 지정(열끼리 합친다.)
-<tr>: 표 내부의 행
-<th>: 행 내부의 제목 셀
-<td>: 행 내부의 일반 셀
