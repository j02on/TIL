1. 변수
-var: 변수 선언을 의미하는 키워드
      var a = 1;
*;(세미콜론): 문장 맨 뒤에 붙이는 세미콜론은 문장의 끝을 의미
              js엔진은 소스 해석 도중 세미콜론을 만나면 하나의 문장이 끝나는 곳이라고 생각하여 다음 줄로 이동해 다른 코드를 해석하기 시작
-변수를 여러 개 지정
    1. var를 이용해 여러 줄로 변수 구분을 하는 경우
    ex) var a = 1;
        var b = 2;
    2. 콤마(,)를 이용해 변수 구분을 하는 경우
    ex) var a = 1[, b = 2];
        var name = "ddss", age = 30;
    *[]란?: 내부에 들어있는 내용을 생략해도 된다는 의미

-변수 이름 만들 때 주의사항
      1. 숫자로 시작하면 안된다.
      2. 대소문자가 구분됨(name과 NAME은 다른 변수)
      3. 변수는 대문자가 아닌 소문자로 시작
      4. 변하지 않는 환경 변수의 값을 담는 상수 변수는 모두 대문자로 만들어야 함
      5. 여러 단어가 조합될 때는 낙타 표기법으로 작성(nameuser -> nameUser)
      6. js에서 이미 정의된 예약어를 사용하면 x

-변수에 저장할 수 있는 데이터 종류
      1. 데이터 위치
      2. 데이터 종류(number, boolean, string, function, class, class instance, undefined, null)
      -number: 정수형, 실수형으로 나뉨, 정수형 10진수, 정수형 16진수, 실수형이 있음
      -string: 문자형
      -boolean: 참과 거짓
      -undefined: 변수를 생성 후 초기화하지 않은 상태
      -null: 아무것도 참조하고 있지 않다, 주로 객체 담을 변수 초기화할 때 사용
- 변숫값 저장 및 변경

*변수에는 상수뿐 아니라 또 다른 변수 자체를 저장할 수 있다. 
-> var 변수1 = 데이터;
   var 변수2 = 변수1;
   var 변수
